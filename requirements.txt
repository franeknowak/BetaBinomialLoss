# Verified together on Python 3.10 with the CUDA 12.1 PyTorch build.
# NumPy is held below 2.0: scikit-learn 1.3.2 is compiled against the NumPy 1.x
# ABI and fails to import under NumPy 2.x.
# For a different CUDA build, install torch/torchvision first from
# https://pytorch.org/get-started/locally/ then run this file.

# Training
torch==2.4.1
torchvision==0.19.1
timm==1.0.26

# Data handling and configuration
numpy==1.26.4
pillow==10.1.0
PyYAML==6.0.1
yacs==0.1.8

# Metrics
scikit-learn==1.3.2

# Results summaries (results/generate_summary.py)
pandas==2.3.3
matplotlib==3.10.9
